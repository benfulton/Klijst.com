<h2 class="title"><%= @lijst.name %></h2>

<%= link_to "Add tings", new_lijst_ting_path(@lijst) %>

<ul id='tings'>
  <% @lijst.tings.each do |ting| %>
  <li>
    #<%= ting.id %> - <%= link_to ting.name, [@lijst, ting] %>
  </li>
  <% end %>
</ul>
